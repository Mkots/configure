#!/usr/bin/env bash
set -euo pipefail

# Re-runs when the pinned font release changes.
# nerd-fonts release: {{ .versions.nerdFonts }}

# macOS picks up fonts from ~/Library/Fonts without a cache rebuild.
[ "$(uname -s)" = "Linux" ] || exit 0

fc-cache -f "$HOME/{{ .fontDir.linux }}"
