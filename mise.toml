[settings]
lockfile = true

# eza lives in mise/{linux,macos}.toml: upstream ships no macOS binaries,
# so it is built from source there (see install/link.sh)
[tools]
neovim = "0.12.0"
zoxide = "0.9.9"
fastfetch = "2.61.0"
gh = "2.89.0"
starship = "1.24.2"
node = "lts"
rust = "latest"
fzf = "0.71.0"
ripgrep = "15.1.0"
fd = "10.4.2"
